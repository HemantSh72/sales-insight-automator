FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install --production
COPY . .
RUN mkdir -p uploads
EXPOSE 5000
CMD ["node", "index.js"]
```

---

### 6B — Create `.dockerignore` in backend folder:

Create `backend/.dockerignore` and paste:
```
node_modules
.env
uploads
npm-debug.log